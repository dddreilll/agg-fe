# Backend base URL (NestJS app). The kitchen display connects to `${VITE_API_URL}/kitchen`.
VITE_API_URL=http://localhost:3000

# Internal store id to subscribe to on load. Defaults to the seeded dev store.
VITE_STORE_ID=d3b07384-d113-4c4e-9c8e-a20468307d14
